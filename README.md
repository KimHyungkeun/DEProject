# Data Engineering Test Project

## 1. 데이터 엔지니어링 테스트 프로젝트

- 주차별로 추출(Extract), 적재(Load), 변환(Transform)을 주제로 테스트를 진행할 공간입니다.

### 1. week1 (2026.02.24 ~ 2026.03.02)
- csv를 이용한 기본적인 ELT 플로우 예시
- 원천(source)에서 데이터를 불러 랜딩(landing)에 임시저장 후, 최종적으로 목적지(target)에 데이터를 저장

### 2. week2 (2026.03.03 ~ Non-Determind)
- (계획중) REST API를 원천(source)으로 사용
- (계획중2) RDBMS를 원천(source)으로 사용